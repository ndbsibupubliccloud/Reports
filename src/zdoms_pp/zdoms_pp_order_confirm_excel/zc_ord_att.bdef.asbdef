projection;
strict ( 2 );
use draft;

define behavior for ZC_ORD_ATT //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action uploadExcelData;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use association _ordtable { create; with draft; }
}

define behavior for ZC_ORD_TBL //alias <alias_name>
{
  use update;
  use delete;

  use association _excel { with draft; }
}